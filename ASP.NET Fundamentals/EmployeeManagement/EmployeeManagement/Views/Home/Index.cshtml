@model IEnumerable<Employee>
@{
    //Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "index Employee list";
}
@*<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
        </tr>
    </thead>
    <tbody>

        @foreach (var employee in Model)
        {
            <tr>
                <td>@employee.ID</td>
                <td>@employee.Name</td>
                <td>@employee.Email</td>
            </tr>
        }

    </tbody>
</table>*@


<div class="card-deck">
    @foreach(var employee in Model)
            {
               <div class="card m-3 d-inline-block w-300" >
                   <div class="card-header">
                       <h3>@employee.Name</h3>
                   </div>
                   <img class="card-img-top" src="~/images/group-18_2.png" asp-append-version="true"/>
                   <div class="card-footer text-center">

                       <a asp-controller="home" asp-action="details" asp-route-id="@employee.ID"
                          class="btn btn-primary">View</a>

                       <a href="#" class="btn btn-primary">view</a>
                       <a href="#" class="btn btn-primary">Edit</a>
                       <a href="#" class="btn btn-primary">Delete</a>
                   </div>
               </div>
            }
</div>

